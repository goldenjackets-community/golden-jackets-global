# Security

## When to Apply
When editing authentication or access controls.

## Rules
- Access restricted to Global Admin only
- Never expose AWS credentials or account IDs in client-side code
- Chapter leads should NOT have access to other chapters' data
- No member PII (emails, names) stored in this repo
